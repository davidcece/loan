package com.cece.lms.repository;

import com.cece.lms.entity.ScoringApplication;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScoringApplicationRepository extends JpaRepository<ScoringApplication, Integer> {
}
